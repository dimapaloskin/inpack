const main = require('preinstalled-main');
const helpers = require('preinstalled-helpers');
const superModule = require('preinstalled-super-module');

module.exports = {
  main,
  helpers,
  superModule
};
